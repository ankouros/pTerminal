# Test Reports

This folder stores test result files and reports generated by pTerminal test
targets.

## Layout

- `run-<timestamp>/`: test run outputs (logs + summary).

Test runs are created by `make tests.report` or `make samakia.testing.verify`.
Do not store secrets here.
